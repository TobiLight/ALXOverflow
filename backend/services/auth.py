


async def hash_password():
    """
    Hashes a user's password and returns the hashed password
    
    Args:
		password (str): User password
  
	Return:
		hashed_password (str): The hashed value of the user password
    """
    hashed_password = ''